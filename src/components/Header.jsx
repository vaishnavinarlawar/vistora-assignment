import React from 'react';

const Header = () => {
    // Render the header with the logo
    return (
        <header className="header">
            <div className="logo">
                <h1>ShopMart</h1>
            </div>
        </header>
    );
};

export default Header;
